# 03 - API Functions

Projet Blazor WASM avec une API Azure Functions (isolated worker). La page `/products` appelle `/api/products`.

```bash
# Lancer le client
cd Client
dotnet run

# Lancer l'API (dans un autre terminal)
cd Api
func start
```
