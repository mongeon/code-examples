# 04 - Authentication

Authentification intégrée de SWA avec Blazor. Inclut un `AuthenticationStateProvider` custom, le parsing du header `x-ms-client-principal` côté API, et un projet Shared pour les modèles communs.
